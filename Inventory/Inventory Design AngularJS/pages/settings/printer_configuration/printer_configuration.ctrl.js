var scotchApp = angular.module('scotchApp.PrinterConfiguration', ['ngRoute']);

// create the controller and inject Angular's $scope
scotchApp.controller('PrinterConfigurationController', function ($rootScope, $scope, $http, $location, $routeParams, $filter) {

    $scope.Services = "PrinterConfiguration Data";










});//last